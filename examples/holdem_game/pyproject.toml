[project]
name = "holdem-game"
version = "0.1.0"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = [
    "holdem_core/**/*.py",
    "holdem_reward.py",
    "holdem_reward_config.py",
]
